# Daily Report — 2026-04-13

## 今日やったこと

### プラン体系の再設計 & マネタイズ強化

#### 背景
FreeプランがPro（¥980/月）への移行動機になっていなかったため、プラン体系を抜本的に見直した。

---

### 1. 料金プラン改定

| プラン | 旧 | 新 |
|--------|----|----|
| Free | テキスト月30回 / ブログ月5本 | テキスト・ブログ合算 **月5回** |
| Pro | 無制限 | 無制限 + **店舗ルール** + **過去投稿メモリ** |
| Team | Pro機能（複数人） | 変更なし |

**変更ファイル:**
- `backend/app/config.py` — `free_monthly_generations: 30 → 5`
- `backend/app/services/usage_service.py` — ブログも `text_generation_count` に合算、`get_usage_summary` を簡略化

---

### 2. Pro限定機能①: 店舗ルール（タグ付け）

サロン単位でNGワード・必須ワード・トンマナ等を登録し、AI生成プロンプトに自動合成。

**実装内容:**
- `backend/migrations/011_salon_rules.sql` — `ALTER TABLE salons ADD COLUMN rules JSONB`（Supabase適用済み✅）
- `backend/app/models/salon.py` — `rules = Column(JSON)` 追加
- `backend/app/schemas/salon.py` — `SalonCreate/Update/Response` に `rules` 追加
- `backend/app/services/prompt_engine.py` — `build_salon_context()` にPro/Team限定で店舗ルール合成ロジック追加
- `frontend/src/types/index.ts` — `SalonRule` 型・`Salon`/`SalonCreateInput` に `rules` 追加
- `frontend/src/pages/SalonSetupPage.tsx` — 店舗ルールUI追加（プリセットタグ: NGワード/必須ワード/トンマナ/ブランドガイド/その他 + 値入力。Freeはロック表示）

---

### 3. Pro限定機能②: 過去投稿メモリ（UI露出）

バックエンドは `content_service.py:get_past_contents()` で実装済みだったため、フロントのトグルをPro/Teamデフォル ONに変更。

**変更ファイル:**
- `frontend/src/pages/GeneratePage.tsx` — `useEffect` で Pro/Team ユーザーのデフォルトを `usePastContents=true` に設定

---

### 4. Stripe未実装期間の導線整備

Stripe決済は未構築のため、ローンチ前の暫定対応として：

**LP (2箇所)**
- `lp/index.html` — Pro CTA を "Coming Soon" 非活性ボタンに差し替え、Team の mailto を `info@kuroco.team` に統一、Free→5回表記
- `frontend/src/pages/LandingPage.tsx` — 同様 + Pro 機能一覧に「🏷️ 店舗ルール」「📚 過去投稿のAI記憶」を追加、オーバーレイ文言を「Pro / Teamプランは現在準備中」に更新

**アプリ内**
- `frontend/src/pages/BillingPage.tsx` — Stripe Checkout/Portal を完全削除。機能比較表は残しつつ、アクションボタンを `mailto:info@kuroco.team` のお問い合わせリンクに差し替え。「Proプランは現在準備中」バナー追加
- `frontend/src/components/billing/UpgradeBanner.tsx` — Checkout廃止 → `/billing` 誘導 + KUROCO問合せ案内に変更
- `frontend/src/components/layout/MainLayout.tsx` — サイドバー「プラン・お支払い」→「プラン・お問い合わせ」に変更

---

## 次にやること（TODO）

- [ ] Stripe の price_id 設定 & Checkout フロー有効化（Proプランローンチ時）
- [ ] `/billing` の "Coming Soon" バナーを Stripe 有効化後に本物の決済ボタンに差し替え
- [ ] 店舗ルールの動作確認（本番環境でサロン編集 → 生成 → `prompt_used` に反映されるか）
- [ ] Pro プラン実際のユーザーで過去投稿メモリのONデフォルト動作確認

---

## 技術メモ

- `blog_generation_count` カラムは後方互換のため残置（今後 increment されない）
- バックエンドの `stripe_service.py` / `routers/billing.py` は削除せず残置（Stripe有効化時にそのまま使える）
- お問い合わせ先: **KUROCO株式会社** `info@kuroco.team`
